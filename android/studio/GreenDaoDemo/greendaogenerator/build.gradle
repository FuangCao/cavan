apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile('de.greenrobot:DaoGenerator:1.3.0')
}

task createDocs {
    def docs = file("/tmp/123")
    docs.mkdirs()

    run {
    }
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
