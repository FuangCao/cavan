apply plugin: 'application'

dependencies {
    compile('de.greenrobot:DaoGenerator:1.3.0')
}

run {
    mainClassName = "com.cavan.GreenDaoGenerator"
    args rootDir.absolutePath + "/app/src/main/java-gen"
}

jar.dependsOn 'run'

targetCompatibility = '1.7'
sourceCompatibility = '1.7'