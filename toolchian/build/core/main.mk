ROOT_PATH = $(shell pwd)
BUILD_PATH = $(ROOT_PATH)/build
PACKAGE_PATH = $(ROOT_PATH)/package
PATCHS_PATH = $(ROOT_PATH)/patchs

CONFIG_FILENAME = configure

WORK_PATH = $(ROOT_PATH)/work
SRC_PATH = $(WORK_PATH)/src
SYSROOT_PATH = $(WORK_PATH)/sysroot
UTILS_PATH = $(WORK_PATH)/utils
DECOMP_PATH = $(WORK_PATH)/decomp
MARK_PATH = $(WORK_PATH)/mark
OUT_PATH = $(WORK_PATH)/out

BUILD_CORE = $(BUILD_PATH)/core
BUILD_TOOLCHIAN = $(BUILD_PATH)/toolchian
BUILD_UTILS = $(BUILD_PATH)/utils

MARK_TOOLCHIAN = $(MARK_PATH)/toolchian
MARK_UTILS = $(MARK_PATH)/utils

MARK_TOOLCHIAN_READY = $(MARK_TOOLCHIAN)/ready
MARK_UTILS_READY = $(MARK_UTILS)/ready

MAKEFILE_TOOLCHIAN = $(BUILD_TOOLCHIAN)/main.mk
MAKEFILE_UTILS = $(BUILD_UTILS)/main.mk
MAKEFILE_DEFINES = $(BUILD_CORE)/defines.mk
MAKEFILE_HOST_RULE = $(BUILD_UTILS)/rule.mk
MAKEFILE_TOOLCHIAN_RULE = $(BUILD_TOOLCHIAN)/rule.mk


PATH := $(SYSROOT_PATH)/bin:$(SYSROOT_PATH)/bin:$(SYSROOT_PATH)/usr/bin:$(SYSROOT_PATH)/usr/sbin:$(SYSROOT_PATH)/usr/local/bin:$(SYSROOT_PATH)/usr/local/sbin:$(PATH)
PATH := $(UTILS_PATH)/bin:$(UTILS_PATH)/bin:$(UTILS_PATH)/usr/bin:$(UTILS_PATH)/usr/sbin:$(UTILS_PATH)/usr/local/bin:$(UTILS_PATH)/usr/local/sbin:$(PATH)

export ROOT_PATH PACKAGE_PATH BUILD_PATH PATCHS_PATH
export SRC_PATH SYSROOT_PATH UTILS_PATH DECOMP_PATH MARK_PATH PATH OUT_PATH
export BUILD_CORE BUILD_TOOLCHIAN BUILD_UTILS
export MARK_TOOLCHIAN MARK_UTILS
export MAKEFILE_DEFINES MAKEFILE_HOST_RULE MAKEFILE_TARGET_RULE MAKEFILE_TOOLCHIAN_RULE

include $(MAKEFILE_DEFINES)

all: build_env $(MARK_TOOLCHIAN_READY)
	$(Q)echo OK

$(MARK_TOOLCHIAN_READY): $(MARK_UTILS_READY)
	$(Q)+make -f $(MAKEFILE_TOOLCHIAN)
	$(call generate_mark)

$(MARK_UTILS_READY):
	$(Q)+make -f $(MAKEFILE_UTILS)
	$(call generate_mark)

clean:
	$(Q)rm $(SRC_PATH) $(SYSROOT_PATH) $(UTILS_PATH) $(MARK_PATH) $(DECOMP_PATH) $(OUT_PATH) -rf

build_env:
	$(Q)mkdir $(SRC_PATH) $(SYSROOT_PATH) $(UTILS_PATH) $(MARK_PATH) $(OUT_PATH) $(DECOMP_PATH) -pv
	$(Q)mkdir $(MARK_TOOLCHIAN) $(MARK_UTILS) -pv

.PHONY: build_env
